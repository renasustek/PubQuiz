  spring:
    application:
      name: PubQuiz
    datasource:
      url: jdbc:mysql://localhost:3306/pubQuiz
      username: renas
      password: ustek
      driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      show-sql: true
      hibernate:
        # 'update' automatically updates your DB schema if you add new fields to your Java entities.
        # If you want your schema.sql to be the ONLY thing that touches the DB, change this to 'validate' or 'none'.
        ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQLDialect
          format_sql: true
    data:
      redis:
        host: localhost
        port: '6379'